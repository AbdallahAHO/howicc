{
  "name": "@howicc/provider-codex",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Future Codex provider adapter for HowiCC",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@howicc/canonical": "workspace:*",
    "@howicc/parser-core": "workspace:*"
  },
  "devDependencies": {
    "@howicc/typescript-config": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
