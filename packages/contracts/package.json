{
  "name": "@howicc/contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared API contracts for the HowiCC API",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.3.0",
    "hono": "^4.12.14",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@howicc/typescript-config": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
