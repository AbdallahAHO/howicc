{
  "name": "@howicc/provider-claude-code",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Claude Code provider adapter for HowiCC",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@howicc/canonical": "workspace:*",
    "@howicc/model-pricing": "workspace:*",
    "@howicc/parser-core": "workspace:*",
    "@howicc/provider-shared-artifacts": "workspace:*"
  },
  "devDependencies": {
    "@howicc/profile": "workspace:*",
    "@howicc/typescript-config": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
