# Public Astro variables.
# Copy to `apps/web/.env.local` for local overrides.

PUBLIC_PRODUCT_NAME="${PRODUCT_NAME}"
PUBLIC_SITE_URL="${PUBLIC_SITE_URL}"
PUBLIC_API_URL="${PUBLIC_API_URL}"
API_SERVER_URL="${API_SERVER_URL}"
